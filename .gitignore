node_modules/
.DS_Store
Thumbs.db

# Local secrets / env files — this repo is public; never commit these.
# Publishing auth comes from CI (OIDC), so no token should ever live on disk.
.env
.env.*
!.env.example
